# Core runtime dependencies
# (the pipeline itself only uses the Python standard library --
# asyncio, dataclasses, logging, collections -- by design, so that
# the core lesson isn't obscured by third-party framework code)

# Development / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
mypy>=1.9.0
ruff>=0.4.0
black>=24.0.0

# Optional: benchmark chart generation
matplotlib>=3.8.0
